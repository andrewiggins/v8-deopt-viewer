// TODO: Write publish script

// 1. Copy the root Readme into the v8-deopt-viewer package
// 2. Update the versions of all the nested npm packages
// 3. Update the dependency versions of nested npm packages
// 4. Run `npm publish` in each sub package

// Options:
// - Specify new version
// - dry run (don't actually publish) (check if `npm publish` has an option for this)
